// Placeholder — real implementation lands in Task 9 (issue #672). Wired here
// only so App.tsx's /profile route tree (Task 2) typechecks and renders.
// Remove this stub once ProfileOverviewPage.tsx is implemented for real.
export default function ProfileOverviewPage() {
  return null;
}
