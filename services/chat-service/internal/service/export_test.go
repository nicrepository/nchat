package service

// ExportLinkSafetyFingerprint lets the external tests recompute the binding a
// write path must have derived from the body it persisted.
var ExportLinkSafetyFingerprint = linkSafetyFingerprint
