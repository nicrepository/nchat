package storage

// ExportMaxTerminalizeBatch lets the deadline tests build a backlog one larger
// than a sweep pass, so the claim's own predicate is what is proved.
const ExportMaxTerminalizeBatch = maxTerminalizeBatch
