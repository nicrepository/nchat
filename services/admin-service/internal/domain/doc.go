// Package domain holds the administrative authorization model: capabilities,
// the principal that holds them, the administrative session, and the audit
// event. Nothing here talks to a database or an HTTP request.
package domain
