# Grafana Dashboards

Dashboard definitions (JSON files) will be added in **TASK-19**.

This directory holds the Grafana dashboard provisioning config.
When TASK-19 is executed, add dashboard JSON files here and a `dashboards.yml` provisioning config.
