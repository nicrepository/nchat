# Unsealed local secrets

Put local plaintext Secret manifests here only long enough to seal them with `kubeseal`.

This directory is ignored by Git except for `.gitkeep` and this README.

Never commit files from this directory.
