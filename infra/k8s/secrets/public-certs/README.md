# Public certificates cache

This directory may hold local cached public certificates fetched from the Sealed Secrets controller.

The certificate is public, but this directory is ignored by default to avoid confusion with private key material.

Never place controller private keys here.
