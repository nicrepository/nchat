apiVersion: v1
kind: Secret
metadata:
  name: nchat-secrets
  namespace: nchat
type: Opaque
stringData:
  POSTGRES_USER: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  VALKEY_PASSWORD: "REPLACE_ME"
  JWT_SIGNING_KEY: "REPLACE_ME"
  FILE_ENCRYPTION_KEY: "REPLACE_ME"
