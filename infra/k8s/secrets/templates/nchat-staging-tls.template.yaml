apiVersion: v1
kind: Secret
metadata:
  name: nchat-staging-tls
  namespace: nchat-staging
type: kubernetes.io/tls
stringData:
  tls.crt: "REPLACE_WITH_CERTIFICATE_PEM"
  tls.key: "REPLACE_WITH_PRIVATE_KEY_PEM"
