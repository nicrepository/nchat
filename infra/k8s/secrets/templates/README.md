# Secret templates

Copy templates from this directory into `infra/k8s/secrets/unsealed/`, replace placeholders locally, and seal them with `scripts/secrets/sealed-secrets-seal.sh`.

Templates are examples only and must not be referenced by Kustomize.
