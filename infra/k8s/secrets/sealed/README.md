# Sealed secrets

Commit encrypted `SealedSecret` manifests here after generating them with strict scope and reviewing the target namespace, Secret name, and owner.

Do not commit plaintext Kubernetes `Secret` manifests in this directory.
