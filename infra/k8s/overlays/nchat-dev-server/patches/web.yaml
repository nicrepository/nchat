apiVersion: apps/v1
kind: Deployment
metadata:
  name: nchat-web
  namespace: nchat
spec:
  template:
    spec:
      containers:
        - name: web
          envFrom:
            - configMapRef:
                name: nchat-config
