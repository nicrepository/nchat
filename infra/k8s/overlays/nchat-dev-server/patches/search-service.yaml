apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: nchat
spec:
  template:
    spec:
      containers:
        - name: search-service
          envFrom:
            - configMapRef:
                name: nchat-config
