apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: nchat
spec:
  template:
    spec:
      containers:
        - name: admin-service
          envFrom:
            - configMapRef:
                name: nchat-config
