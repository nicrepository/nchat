apiVersion: v1
kind: ResourceQuota
metadata:
  name: nchat-dev-quota
  namespace: nchat
spec:
  hard:
    requests.cpu: "4"
    requests.memory: 6Gi
    limits.cpu: "8"
    limits.memory: 12Gi
    pods: "30"
    services: "20"
    persistentvolumeclaims: "5"
    requests.storage: 110Gi
